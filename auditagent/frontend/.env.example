# Vercel: set this in the project's environment variables settings -
# the real deployed Render backend URL. Local dev: leave unset, falls
# back to http://localhost:8000 automatically.
VITE_API_BASE_URL=
